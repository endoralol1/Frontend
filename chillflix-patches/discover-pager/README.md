# Discover professional pager

Replaces minimal `1 / N` pagination with:
- First / prev / next / last
- Windowed page numbers + ellipsis
- Jump-to-page form

CSS lives in `app-docksearch-1232.css` (`?v=20260814-pager1`).
