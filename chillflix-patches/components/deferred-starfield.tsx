import { StarfieldBackground } from "@/components/starfield-background"

/** Immediate static sky — no deferred storm canvas chunk. */
export function DeferredStarfield() {
  return <StarfieldBackground />
}
